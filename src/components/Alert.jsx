import "../App.jsx"

const Alert = (props) => {


    if (count >= 10) {
        setCount(count - 10)
    }
    else {
        Alert("You can't afford that!")
    }
}
        
    
    


export default Alert 