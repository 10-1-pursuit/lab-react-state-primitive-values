import { useState } from "react"
import React from "react";
import "./App.css";

function App() {
  const [count, setCount] = useState[0]
  const buttonOne = documnent.querySelector("button")
  buttonOne.addEventListener("click", (event) => {




  })
  return (
    <div className="card">
      <button onClick={(sythEvent) => count + 1}> count is {count};
      </button>

    </div>


  );
}

export default App;
